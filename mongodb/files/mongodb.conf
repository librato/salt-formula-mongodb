{%- from "mongodb/map.jinja" import config with context %}
# mongodb.conf
# Managed by Saltstack - Do not manually change

# for documentation of all options, see:
#   http://docs.mongodb.org/manual/reference/configuration-options/

{{ config | to_yaml }}
